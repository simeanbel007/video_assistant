# 学习通视频自动播放浏览器插件开发文档

> **文档性质**：技术方案与架构设计  
> **适用范围**：Chromium 内核浏览器（Edge、Chrome 等）  
> **核心目标**：以高隐蔽性方式实现学习通视频的自动连播，并提供实时状态图形化控制界面  
> **安全原则**：前端隐身、不碰触平台反作弊红线、用户完全可控

---

## 目录

1. 整体架构
    
2. 组件详解
    
    - Content Script（内容脚本）
        
    - Service Worker（后台枢纽）
        
    - Popup（用户界面）
        
    - Badge（扩展图标徽标）
        
3. 核心功能模块设计
    
    - 视频检测与状态上报
        
    - 前台伪装与保活机制
        
    - 自动播放控制与切换策略
        
    - 拟人化行为仿真
        
    - 弹窗验证感知与暂停
        
4. 图形界面设计
    
    - Popup 界面布局
        
    - 状态指示灯逻辑
        
    - 开关交互流程
        
5. 数据流与状态管理
    
6. 隐蔽性设计清单
    
7. 扩展权限与清单文件参考
    
8. 异常处理与边界情况
    
9. 未来可扩展性
     
10. 遗漏考虑与补充建议

11. AI 答题助手模块设计（规划中）

---

## 整体架构

采用标准的三层通信架构，确保用户界面与页面逻辑完全隔离，学习通页面端无法感知扩展内部状态：


┌────────────────────┐
│   学习通页面        │
│   (Content Script) │ ← DOM 操作、事件监听、行为仿真
└────────┬───────────┘
         │ chrome.runtime.sendMessage
┌────────▼───────────┐
│   Service Worker   │ ← 状态缓存、指令转发、持久存储
└────────┬───────────┘
         │ chrome.runtime.sendMessage + onMessage
┌────────▼───────────┐
│   Popup (界面)     │ ← 只读展示 + 用户操作下发
│   Badge (徽标)     │ ← 静默状态提示
└────────────────────┘

- **通信通道**：全部通过 `chrome.runtime.sendMessage`，隔绝于页面 JS 上下文。
    
- **状态持久化**：使用 `chrome.storage.local` 保存用户开关配置。
    
- **安全边界**：Popup 和 Service Worker 的 DOM 不受页面检测；Content Script 运行于隔离世界。
    

---

## 组件详解

### Content Script（内容脚本）

- **注入时机**：`document_idle`，确保 DOM 加载完成。
    
- **运行环境**：隔离的“主世界”（`world: 'ISOLATED'`），变量对页面不可见。
    
- **职责**：
    
    1. 检测视频播放器的出现与消失。
        
    2. 监听播放相关事件（`play`、`pause`、`ended`）。
        
    3. 执行前台伪装逻辑（重写部分 API）。
        
    4. 实现自动切换/连播逻辑。
        
    5. 生成拟人化鼠标、键盘事件。
        
    6. 监视弹窗验证元素。
        
    7. 向上报告视频状态、页面变化。
        

### Service Worker（后台枢纽）

- **生命周期**：扩展安装后常驻，按需唤醒。
    
- **职责**：
    
    1. 缓存最新视频检测状态、开关状态。
        
    2. 转发 Content Script 与 Popup 之间的消息。
        
    3. 持久化开关配置（`chrome.storage.local`）。
        
    4. 更新扩展图标 Badge。
        

### Popup（用户界面）

- **承载形式**：`default_popup` 定义的独立 HTML 页面。
    
- **生命周期**：仅在用户点击扩展图标时创建，失去焦点后自动销毁。
    
- **职责**：
    
    1. 向后台请求当前状态并显示。
        
    2. 提供自动播放开关、状态指示灯。
        
    3. 可能显示附加信息（视频标题、切换次数等）。
        

### Badge（扩展图标徽标）

- **更新源**：Service Worker 在状态变化时设置。
    
- **显示规则**：
    
    - 绿色背景 + “ON”：视频已检测到且自动播放启用。
        
    - 橙色背景 + “！”：视频已检测到但自动播放暂停。
        
    - 灰色背景 + “OFF”：未检测到视频或扩展未激活。
        

---

## 核心功能模块设计

### 视频检测与状态上报

**检测策略**：

- 使用 `MutationObserver` 监视页面 `body` 变化，过滤新增的 `<video>` 元素。
    
- 辅助校验：检查 `<video>` 是否位于学习通专用播放器容器内（典型特征：`class` 包含 `video-js`、`vjs` 或特定 `id`）。
    
- 当视频元素出现且 `src` 有效时，上报 `{type: 'video_detected', info: {title, duration, ...}}`。
    
- 监听 `ended`、视频 `remove` 事件，当页面没有合法视频超过阈值时间（如 3 秒），上报 `{type: 'video_lost'}`。
    

**避免误判**：

- 忽略幻灯片、音频、广告等非学习主视频元素。
    
- 可通过节点树深度和祖先元素特征做精确匹配。
    

### 前台伪装与保活机制

目的：在浏览器标签页被切到后台、窗口最小化时，仍能正常播放并累积有效时长，同时不让学习通脚本感知到真正的隐藏状态。

**技术组合：**

1. **`document.hidden` 与 `visibilityState` 重写**
    
    javascript
    
    // 原理性示例（实行需谨慎）
    `Object.defineProperty(document, 'hidden', { get: () => false });`
    `Object.defineProperty(document, 'visibilityState', { get: () => 'visible' });`
    
    阻止页面通过这两个属性判断失焦。
    
2. **拦截 `visibilitychange` 事件**  
    对 `addEventListener` 进行包装，阻止“visibilitychange”相关监听器的注册，或使其回调永远接收伪造的可见状态。
    
3. **画中画（PIP）强制激活**  
    当检测到视频播放时，自动调用 `video.requestPictureInPicture()`。即使原标签页被切走，PIP 窗口会保持播放且浏览器视为活跃。  
     – 注意：PIP 窗口可能会被用户手动关闭，需监听 `leavepictureinpicture` 事件并重新触发。
    
     – ⚠️ **重要限制**：`video.requestPictureInPicture()` 在 Chromium 中必须在**用户手势**（user gesture）上下文中调用（如 click、keypress 事件回调）。程序化自动调用会被浏览器拒绝。如果无法获取用户手势，需回退到仅依赖 API 重写保活。
    
4. **防止定时器降频**  
    后台标签页的 `setInterval`/`setTimeout` 会被限制至最小 1000ms 延时。扩展可通过 Service Worker 向 Content Script 发送高频心跳消息，在页面内创建 `requestAnimationFrame` 或微任务循环保持活跃度。
    

### 自动播放控制与切换策略

**触发机制**：当“自动播放”开关开启时，Content Script 接管控制。

**三级切换策略（逐级尝试）：**

1. **首选：点击“下一节”按钮**
    
    - 定位按钮：通过文本内容（“下一节”、“继续”、“下一讲”）、`class`/`aria-label` 等。
        
    - 使用 `element.dispatchEvent(new MouseEvent('click', {...}))` 模拟点击。
        
    - 大多数普通按钮不会检查 `isTrusted`，故此方法覆盖率约 80%。
        
2. **备选：键盘模拟**
    
    - 模拟空格键、回车键事件，或平台自定义快捷键。
        
    - `element.dispatchEvent(new KeyboardEvent('keydown', {key: 'Enter', ...}))`。
        
3. **兜底：直接导航**
    
    - 解析当前课程大纲 DOM，提取下一未完成章节的绝对路径。
        
    - 使用 `window.location.href` 跳转，并依赖 Content Script 重新注入。
        

**章节列表预抓取**：

- 进入课程页时，一次性抓取侧边栏所有未完成视频节点的 `href` 或 `data-id` 存入数组，按序推进，减少频繁 DOM 查询。
    

**播放完成的判定**：

- 以视频的 `ended` 事件为主，辅以进度条 `currentTime >= duration - 2s` 作为兜底，防止事件丢失。
    

### 拟人化行为仿真

模拟人类用户在观看视频时的无意识操作，使行为日志难以与脚本区分。

**仿真项目清单：**

1. **随机鼠标移动**
    
    - 每分钟随机触发 2~4 次。
        
    - 使用贝塞尔曲线生成路径，分 10~20 帧派发 `mousemove` 事件。
        
    - 起点和终点偏移随机，速度加入波动。
        
2. **无害点击**
    
    - 随机在播放器周边空白区域派发 `click` 事件（1~2 次/视频）。
        
    - 可偶尔点击进度条附近但立即移开，模仿犹豫。
        
3. **音量/速度微调**
    
    - 每 10~20 分钟随机调整音量 ±3%，再调回。
        
    - 短暂调出控制栏（通过 `mousemove` 到播放器底部），1~2 秒后移走。
        
4. **模拟切换行为**
    
    - 随机触发一次 `visibilitychange` 事件（用 `dispatchEvent`），并在 3~8 秒后再触发恢复，同时伴随鼠标移动。模仿用户临时切走看通知，然后回来。
        
5. **键盘交互**
    
    - 每分钟随机派发一次方向键事件（但不对页面产生实际影响），增加事件类型多样性。
        

**随机化参数表**：  
所有间隔、移动距离、点击位置均由带高斯噪声的随机函数生成，避免定时模式。

### 弹窗验证感知与暂停

学习通视频中途可能弹出滑块、算术题、选择题等验证，必须停止自动化，提醒用户处理。

**检测机制**：

- `MutationObserver` 监视 `body`，检测新增节点中是否包含特定关键词：
    
    - “滑块验证” / “请完成验证”
        
    - “计算结果” / “以下哪个选项正确”
        
    - 特殊的弹窗类名（如 `.yidun_modal`, `.captcha-popup`）
        
- 辅助判断：弹窗是否位于页面中央、带有遮罩层（`z-index` 极高）。
    

**响应动作**：

1. 立即暂停一切自动切换定时器、鼠标仿真循环。
    
2. 通过 `chrome.runtime.sendMessage` 告知 Service Worker，更新 Badge 为警告图标。
    
3. 通过 `chrome.notifications` API 发送桌面通知，内容如“检测到验证弹窗，请手动处理”。
    
4. 在 Popup 界面中显示“等待手动处理”的醒目提示（如果用户恰好打开）。
    

**恢复机制**：

- 继续用 `MutationObserver` 监视弹窗节点被移除的事件。
    
- 移除后延迟 5 秒，自动重启仿真和行为循环，并上报已恢复。
    

---

## 图形界面设计

### Popup 界面布局

尺寸：320x240 px（适中，不遮挡过多内容）

text

┌────────────────────────────┐
│  ● 自动播放助手           │
│                            │
│   状态：已连接视频         │
│   视频：视觉文化与社会性别 │
│                            │
│  自动连播  [═══●═══] 开   │
│                            │
│  已自动切换 3 个视频       │
│  下一节：第4讲 性别与凝视 │
│                            │
│  ──────────────────────   │
│  ✓ 行为仿真运行中          │
│  ✓ 前台伪装已激活          │
└────────────────────────────┘

- **顶部**：LED 指示灯（绿/橙/灰圆点） + 标题
    
- **中部**：状态描述、视频信息、主开关滑块
    
- **底部**：附加功能状态及切换次数统计
    
- **配色**：跟随系统深色/浅色模式，减少突兀感
    

### 状态指示灯逻辑

|指示灯颜色|状态含义|触发条件|
|---|---|---|
|绿色常亮|视频已检测到，自动播放已开启|Content Script 上报 video_detected + 开关为 true|
|绿色闪烁|自动播放中，但当前正在切换视频|切换间隙上报|
|橙色常亮|视频已检测到，但自动播放已暂停|开关为 false 或用户手动暂停|
|橙色闪烁|检测到验证弹窗，等待手动处理|弹窗节点出现|
|灰色常亮|未检测到学习通视频页面|当前标签页无有效视频|

### 开关交互流程

1. 用户点击 Popup 中的开关。
    
2. Popup 发送 `{cmd: 'toggle', value: true/false}` 给 Service Worker。
    
3. Service Worker 更新存储，并转发指令给对应的 Content Script（通过 tab ID）。
    
4. Content Script 启用/禁用全自动模块，并回传确认。
    
5. Service Worker 更新 Badge 文字/颜色，并向 Popup 返回最新状态。
    

---

## 数据流与状态管理

**核心状态机（Service Worker 内部）：**

text

[空闲] --(video_detected)--> [有视频] --(开关开启)--> [自动播放中]
                              ↑  ↓                  ↓
                              │ [开关关闭]         [视频播放结束]  
                              │  ↓                  ↓
                              └─[有视频暂停]    [切换视频中]──→[有视频]
                                                      ↓
                                                  [弹窗验证]──→[验证暂停]

**消息类型定义（内部协议）：**

|方向|消息类型|载荷|
|---|---|---|
|上游 (CS -> SW)|`video_detected`|`{title, duration}`|
||`video_lost`|`{}`|
||`popup_detected`|`{type: 'captcha'}`|
||`popup_gone`|`{}`|
||`status_report`|`{active: bool, switch: bool}`|
|下游 (Popup -> SW)|`get_status`|`{}`|
||`toggle_switch`|`{value: bool}`|
|上游响应 (SW -> Popup)|`status_response`|`{hasVideo, autoActive, info, ...}`|

---

## 隐蔽性设计清单

- **不与页面 DOM 共享 UI**：Popup 和 Badge 均不在学习通页面渲染，页面无法感知。
    
- **事件避让 `isTrusted` 检查**：仿真事件不用在关键节点（如作业提交），仅用于非核心交互。
    
- **API 重写最小化影响**：只覆盖必要对象，且通过 `Proxy` 或包装函数保持原生行为，避免触发平台 API 完整性检查。
    
- **Content Script 隔离**：使用 `world: 'ISOLATED'`，脚本变量不被页面读取。
    
- **网络请求收敛**：扩展不向任何外域发送数据（全本地运行），无 CSP 违规风险。
    
- **错误静默处理**：所有自动化操作包裹 `try-catch`，异常不泄漏到控制台（页面可见）。
    
- **代码混淆与扩展伪装**：扩展名称、图标使用普通化文本（如“笔记助手”），避免直接包含“自动播放”等字符串。manifest 权限保持最小。
    
- **无 `web_accessible_resources` 暴露**：防止页面通过资源探测发现扩展。
    

---

## 扩展权限与清单文件参考

`manifest.json`（关键项）：

json
```
{
  "manifest_version": 3,
  "name": "____________", // 使用中性名称
  "version": "1.0",
  "description": "…………",
  "permissions": [
    "storage",
    "notifications",
    "scripting",
    "activeTab"
  ],
  "host_permissions": [
    "*://*.chaoxing.com/*",
    "*://*.edu.cn/*"
  ],
  "background": {
    "service_worker": "sw.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "打开面板"
  },
  "content_scripts": [{
    "matches": ["*://*.chaoxing.com/*", "*://*.edu.cn/*"],
    "js": ["content.js"],
    "run_at": "document_idle"
  }]
}
```

- 不需要 `tabs` 权限，仅用 `activeTab` + 宿主权限即可注入。
    
- `notifications` 权限用于验证弹窗提醒。
    

---

## 异常处理与边界情况

|异常场景|处理方式|
|---|---|
|视频元素突然被移除（页面刷新、跳转）|Content Script 检测到后立即停止模拟，上报 `video_lost`，重置切换队列。|
|自动下一节按钮不存在（课程结构特殊）|三级策略兜底后仍失败，暂停自动化，Badge 变红，通知用户。|
|PIP 被浏览器限制或自动退出|回退为 API 重写保活，并记录日志。|
|`MutationObserver` 检测到大量 DOM 扰动导致性能问题|添加防抖逻辑，合并 100ms 内变化再处理。|
|用户手动操作与自动化冲突|持续监听真实鼠标/键盘事件；如果检测到高频真实操作，自动暂停自动化 2 分钟，转为观察模式。|
|存储同步失败|使用 `chrome.storage.local` 的回调进行错误处理，默认开关为关闭状态保证安全。|

---

## 未来可扩展性

当前架构支持无缝扩展以下模块，而不需重构核心通信：

- **进度统计面板**：在 Service Worker 中累计切换次数、观看时长，显示在 Popup 中。
    
- **多标签页支持**：Service Worker 维持 `tabId → state` 映射，Pop 时用户选择控制哪个标签页。
    
- **自动答题开关模块**：增加新的指令类型，触发题目感知与搜题逻辑，UI 上添加第二个开关。
    
- **计划任务**：设置自动播放总时长或完成章节数，达标后自动停止。

- **AI 答题助手模块**：接入用户自定义 API（如 DeepSeek），实现题目感知与 AI 对话。详细设计见第 11 节。

---

## 11. AI 答题助手模块设计（规划中）

> **状态**：暂未实现，待条件成熟后开发。  
> **暂缓原因**：
> 1. 缺少后端数据库支持，对话历史无法持久化。
> 2. AI API 响应时间可能超过 30 秒，而 Popup 失去焦点后会被自动销毁，消息通道中断。
> 3. 需引入 Service Worker 长连接或 `chrome.offscreen` API 作为代理层解决超时问题。

### 11.1 功能定位

**模式 B：半自动辅助**

- Content Script 检测到页面题目弹窗时，自动抓取题目文本。
- Popup 中展示题目摘要，用户点击"发送给 AI"后发起请求。
- AI 返回答案后显示在聊天框中，用户自主决定是否采纳。
- **全程不自动填入答案**，不替代用户操作。

### 11.2 用户交互流程

```
视频播放中
  → 平台弹出题目（选择题/判断题/填空题）
    → Content Script 抓取题目文本
      → 上报给 SW → 转发给 Popup（如已打开）
        → Popup 显示题目摘要 + [发送给AI] 按钮
          → 用户点击按钮
            → SW 代理请求 → AI API 返回
              → Popup 展示回答
                → 用户手动对照填入页面
```

### 11.3 对话界面布局

```
Popup (320x480)
┌──────────────────────────────┐
│  [ 播放控制 ]  [ AI助手 ① ] │  ← 标签切换，①为题目提示
├──────────────────────────────┤
│  ┌──────────────────────────┐│
│  │ ⚙ API 设置              ││
│  │ 接口地址: [___________]  ││  ← 用户自定义
│  │ API Key:  [___________]  ││
│  │ 模型名称: [___________]  ││
│  └──────────────────────────┘│
│                              │
│  ┌─ 检测到题目 ────────────┐│
│  │ 社会主义核心价值观包括 ││
│  │ 哪些内容？(多选题)     ││  ← 只读展示
│  │            [发送给 AI]  ││
│  └──────────────────────────┘│
│                              │
│  ┌─ 对话记录 ──────────────┐│
│  │ 🙋 这道题选什么？       ││
│  │ 🤖 根据题干，建议选    ││
│  │    A、C、D，理由...    ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │ [输入问题____________]  ││
│  │                   [发送] ││
│  └──────────────────────────┘│
└──────────────────────────────┘
```

### 11.4 数据流

```
popup.js                     sw.js                     外部 API
   │                           │                           │
   │── {type:'ai_chat',        │                           │
   │     messages:[...],       │                           │
   │     config:{url,key,model}}│                          │
   │                           │── fetch(api_url, {...}) →│
   │                           │   headers: Authorization  │
   │                           │   body: {model,messages}  │
   │                           │                           │
   │                           │←── 流式/完整响应 ────────│
   │                           │                           │
   │←── {type:'ai_response',  │                           │
   │      content:'...'}       │                           │
```

### 11.5 待解决问题

| 问题 | 现状 | 可能的方案 |
|------|------|-----------|
| **Popup 生命周期** | 失去焦点即销毁，长请求（>30s）返回时 Popup 已不存在 | ① SW 收到响应后通过 `chrome.action.setBadgeText` 提示用户重新打开 Popup；② 使用 `chrome.offscreen` API 维持一个后台页面接收响应 |
| **对话持久化** | 无数据库，对话记录仅在 Popup 打开期间保留 | 使用 `chrome.storage.local` 存储最近 N 条对话记录（5MB 上限足够数千条短对话） |
| **API 流式响应** | Popup 无法维持长连接接收 SSE | SW 中聚合完整响应后一次性返回；未来可考虑 `chrome.sidePanel` API 保持面板常驻 |
| **API Key 安全** | 纯前端存储有泄漏风险 | 存储于 `chrome.storage.local`（仅扩展自身可读写），不在页面 DOM 中暴露 |
| **host_permissions** | 用户自定义 API 地址不可预知 | SW 代理模式无需 `host_permissions`，或提示用户自行添加 |

### 11.6 涉及改动文件

| 文件 | 改动 |
|------|------|
| `popup.html` | 新增加标签页结构、聊天区域、设置面板 |
| `popup.css` | 标签栏样式、聊天气泡样式 |
| `popup.js` | 标签切换、聊天逻辑、API 配置读写 |
| `sw.js` | 新增 `ai_chat` / `ai_response` 消息类型，代理 fetch |
| `content.js` | 新增题目检测与文本抓取上报 |
| `manifest.json` | 视方案决定是否新增 `host_permissions` |

---

## 10. 遗漏考虑与补充建议

### 10.1 Manifest 配置注意事项

- **`content_scripts` 中不能包含 `world` 字段**：`"world": "ISOLATED"` 只能用于 `chrome.scripting.registerContentScripts()` 或 `chrome.scripting.executeScript()` API，不可写在 `manifest.json` 的 `content_scripts` 声明中。Manifest V3 的 content_scripts 默认即为隔离环境，无需额外指定。
- **本地加载无需 `icons` 和 `default_icon`**：仅开发调试时"加载解压缩的扩展"不强制要求图标配置。如果要提交到 Edge 商店，才必须提供至少 3 种尺寸的图标（16/48/128 px）。
- **`action` 中的 `default_icon` 为可选**：本地使用时缺少图标仅显示默认灰色拼图图标，不影响功能。

### 10.2 Browser Autoplay Policy（自动播放策略）

Chromium 的 **Autoplay Policy** 会阻止带声音的视频自动播放：
- `video.play()` 返回 Promise，不带声音的视频或用户已高频交互过的网站可通过 MEI（Media Engagement Index）获得自动播放权限。
- 建议优先使用 `video.muted = true` + `video.play()` 绕过限制，之后通过模拟用户交互逐步取消静音。
- 务必监听 `video.play()` 的 Promise 拒绝，捕获 `NotAllowedError` 并做降级处理。

### 10.3 Service Worker 生命周期

Service Worker 在空闲约 **30 秒**后会被浏览器强制终止，所有内存状态丢失：
- 在 `chrome.runtime.onInstalled` 中初始化默认开关状态。
- 所有需持久化的状态（开关配置、已观看章节等）必须写入 `chrome.storage.local`，不能依赖内存变量。
- `tabId → state` 映射需要额外容灾机制（SW 唤醒后从 storage 恢复或主动向 Content Script 查询）。

### 10.4 多标签页冲突

用户可能同时打开多个学习通页面：
- Content Script 在每个标签页中独立运行，各自上报状态。
- Badge 状态会因多个标签页相互覆盖而混乱。
- 建议 Service Worker 维护 `Map<tabId, TabState>`，综合所有标签页状态后决定 Badge 显示；当某标签页关闭时（`tabs.onRemoved`）清理对应状态。

### 10.5 MutationObserver 性能与资源清理

- **观察范围过大**：监视整个 `body` 的变化在高频 DOM 变动页面会导致明显性能开销。建议将观察范围缩小到视频播放器容器（如 `.video-js` 或特定 `#iframe` 的父节点）。
- **资源释放**：当视频消失或页面跳转时，必须调用 `observer.disconnect()` 释放绑定，否则造成内存泄漏。
- **防抖**：已有防抖设计，建议明确设为 100~200ms 合并窗口。

### 10.6 跨域 iframe 中的视频

学习通视频经常嵌入在跨域 iframe 中：
- Manifest 声明的 `content_scripts` 默认只注入顶层页面。需要设置 `"all_frames": true` 才能注入所有内嵌框架。
- 跨域 iframe 中的 DOM 仍可通过 Content Script 访问，但顶层页面脚本和 iframe 脚本之间无法直接共享变量，需通过 `window.postMessage` 或 `chrome.runtime.sendMessage` 中转消息。

### 10.7 `chrome.storage.local` 容量限制

- 默认上限为 **5MB**（可申请 `"unlimitedStorage"` 权限扩展）。
- 对于累积观看历史、多标签页状态数据：需定期清理过期 tab 状态，避免存储大体积日志或视频元数据。

### 10.8 扩展更新时的状态迁移

当扩展版本更新时，Service Worker 重启且 `chrome.runtime.onInstalled` 的 `reason` 为 `"update"`：
- 需处理旧版本数据格式迁移。
- 正在运行的内容脚本不会自动刷新——用户需手动刷新页面才能使新脚本生效，可能需要通过通知引导用户。

### 10.9 内容脚本重复注入

SPA 导航或页面局部刷新时 Content Script 可能被重复注入（URL 未变但 DOM 重建）：
- 应使用全局标志（如 `window.__EXTENSION_INITIALIZED__`）防止重复初始化 MutationObserver 和事件监听。
- 每次重新注入时需检查是否已有运行中的观察者/定时器，避免累积。

### 10.10 Badge 配置细节

- `chrome.action.setBadgeBackgroundColor` 的 `color` 参数可使用 CSS 颜色字符串（如 `"orange"`、`"#FFA500"`）或 RGB 数组格式 `[R, G, B, A]`（如 `[255, 165, 0, 255]`）。
- 徽标文字上限为 **4 个字符**（"OFF" 为 3 字符，安全）。
- 无标签页时的 Badge 应显示灰色 "OFF"，避免残留上一条状态。

### 10.11 `notifications` 权限降级

- Windows 专注助手（Focus Assist）或系统的勿扰模式可能全局屏蔽浏览器通知。
- 建议通过 Badge 颜色变化 + Popup 内提示作为通知的降级方案，确保验证弹窗提醒不会因系统设置而丢失。

### 10.12 `document.hidden` 重写的可靠性

- 在 Chromium 中，`document.hidden` / `visibilityState` 可通过 `Object.defineProperty` 在 document 实例上覆盖。
- 但如果平台脚本通过 `Document.prototype.hidden` 的 getter 读取原型值，或使用 `document.defaultView` 来间接判断，则无法被拦截。
- 建议综合使用多种伪装手段（PIP + API 重写 + 心跳激活），而非依赖单一方案。

### 10.13 `isTrusted` 事件的审查风险

- 所有通过 `dispatchEvent` 程序化派发的事件的 `isTrusted` 属性均为 `false`。
- 越来越多的教育平台开始检查关键操作的 `isTrusted` 值。
- 对于核心交互（如提交测验答案），应坚决避免模拟事件，仅引导用户手动操作。

### 10.14 调试与测试建议

- 在 `chrome://extensions/` 页面启用 **"收集错误"** 视图查看扩展抛出的未捕获异常。
- 使用 `chrome.storage.local.get(null, console.log)` 随时检查存储状态。
- 为隐蔽性模块编写单元测试时，可使用 `about:blank` 页面注入 Content Script 独立验证行为仿真逻辑。